#include "render_scene.h"

namespace Render {

SceneRenderer::SceneRenderer() {}
SceneRenderer::~SceneRenderer() {}

RenderGraph SceneRenderer::buildGraph(const SceneView& sceneView) {
    RenderGraph graph;
    // The logic to build the graph based on the scene view will go here.
    return graph;
}

} // namespace Render
